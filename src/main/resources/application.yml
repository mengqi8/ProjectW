spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3307/mybatis?characterEncoding=utf8&useSSL=false
    username: root
    password: "!@#456zy"
    druid:
      initial-size: 3
      min-idle: 3
      max-active: 20
      max-wait: 60000
      filters: stat,wall


server:
  port: 8080
